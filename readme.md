# REST API with react typescript

## GET

`GET` is getting all the data

## POST

`POST` is sending the data

## PUT

`PUT` is to update all the data

## PATCH

`PATCH` is to update only specific data

## CREATE

`CREATE` is to share a common base URL
